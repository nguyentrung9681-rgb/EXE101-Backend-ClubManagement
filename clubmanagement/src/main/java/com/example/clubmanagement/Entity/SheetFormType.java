package com.example.clubmanagement.Entity;

/**
 * Phân loại bắt buộc cho Google Sheet và Google Form khi tạo mới.
 * EVENT           → Dùng cho sự kiện (Event)
 * CLUB_ACTIVITIES → Dùng cho hoạt động câu lạc bộ (Club Activities)
 */
public enum SheetFormType {
    EVENT,
    CLUB_ACTIVITIES
}
